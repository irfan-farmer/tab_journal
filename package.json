{
  "name": "tab_journal",
  "version": "1.1.0",
  "description": "Chrome extension that periodically snapshots open tabs into a searchable, exportable journal so links are never lost.",
  "main": "background.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node tests/parse_js.js",
    "icons": "node scripts/make-icons.js",
    "build": "node scripts/build.js"
  },
  "keywords": [
    "chrome-extension",
    "tabs",
    "history",
    "productivity"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "acorn": "^8.17.0"
  }
}
